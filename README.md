# 🔒 claudemods arch freeze
tool to turn any arch distro immutable
currently not tested only coded but alot of work will be done to this soon

🔒 **Lock system (default state)** `archfreeze-lock`

🔓 **Unlock for maintenance** `archfreeze-unlock`

🔄 **Safe system updates** `archfreeze-update`

📸 **Manage snapshots** `archfreeze-snapshot create my-snapshot` `archfreeze-snapshot list` `archfreeze-snapshot restore my-snapshot`

🚨 **Emergency recovery** `archfreeze-recovery`
